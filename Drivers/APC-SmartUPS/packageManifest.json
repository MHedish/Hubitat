{
  "packageName": "APC SmartUPS Status",
  "author": "Marc Hedish",
  "version": "0.3.6.36",
  "minimumHEVersion": "2.3.0",
  "dateReleased": "2025-11-30",
  "releaseNotes": "RELEASE CANDIDATE — High-performance driver for APC Smart-UPS with Network Management Cards (NMC). Uses a deterministic Telnet lifecycle and transient in-memory context for sub-5-second telemetry without race conditions or timeouts.\n\n0.3.6.35 - Corrected watchdog methods that were causing infinite loops.\n-Added adaptive offset adjustment in scheduleCheck() to align refresh cycles within current hour when offset exceeds 2× interval; corrected ConnectState() debug logging to eliminate double event emission.\nRefined finalizeSession() concurrency guard; added adaptive offset alignment in scheduleCheck() for better refresh synchronization.\n\n0.3.6.36  -- Fixed updateConnectState() to no longer emit events twice; RCA -- Hubitat's asynchronous event queue delay before commit.",
  "documentationLink": "https://github.com/MHedish/Hubitat/blob/main/Drivers/APC-SmartUPS/README.md",
  "changelog": "https://github.com/MHedish/Hubitat/blob/main/Drivers/APC-SmartUPS/CHANGELOG.md",
  "tags": ["UPS","APC","Smart-UPS","Integrations","Monitoring","Hubitat","Tools & Utilities"],
  "drivers": [
    {
      "id": "MHedish-APC-SmartUPS-Status",
      "name": "APC SmartUPS Status",
      "namespace": "MHedish",
      "location": "https://raw.githubusercontent.com/MHedish/Hubitat/refs/heads/main/Drivers/APC-SmartUPS/APC-SmartUPS-Status.groovy",
      "required": true,
      "version": "0.3.6.36"
    }
  ]
}
